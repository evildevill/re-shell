# RE Shell

This is an agent-neutral reverse-engineering workbench. Canonical workflows are in `skills/`; Claude Code discovers those skills directly when this repository is installed as a plugin.

Follow `AGENTS.md` for repository-wide operating rules. Use the most specific matching skill and `skills/general-re/SKILL.md` for shared tool and artifact guidance. Do not create a second copy under `.claude/skills/`.
